function getAge(inputString) {
  // return the girl's correct age as an integer. Happy coding :)
  // parseInto turns parameter into a string
  return parseInt(inputString);
}

// alternate solutions
const getAge = parseInt;
