function numberToString(num) {
  // toString is a method to convert to string.
  return num.toString();
}

// Alternate solution
function numberToString(num) {
  // Return a string of the number here!
  return String(num);
}
