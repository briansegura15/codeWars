Take an array and remove every second element from the array. 
Always keep the first element and start removing with the next element.

P:
what if the array is empty
R:
should return the array of elements with every 2nd element removed
E:
given hello no there should return hello there
P:
create a function that accepts an array as a parameter
filter out the 2nd element of every array
return the new array